.venv/
.pytest_cache/
__pycache__/
*.py[cod]

# Re-cloneable pinned external dependencies; their immutable pins are in docs.
upstream/
repro/vendor/eigen/

# Local build products. Keep only small, auditable JSON evidence.
outputs/eigen-cmake/
outputs/eigen-install/
outputs/run_tree
outputs/benchmark_rank_update
outputs/*.so
GATE_REPORT.json
style_check.json
poster_preview.pdf
poster_preview.png

# Trackio runtime scratch; retain the logbook and metadata.
.trackio/logbook/.sync_*
.trackio/logbook/*.log
